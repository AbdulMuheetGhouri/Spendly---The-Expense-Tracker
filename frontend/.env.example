# Base URL of your Express backend (index.js listens on port 3000).
# CORS in the backend is locked to http://localhost:5173 with credentials,
# so run this frontend with Vite's default port (5173) or update the
# backend's cors() origin to match whatever port you use.
VITE_API_URL=http://localhost:3000
